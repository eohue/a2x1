{
  "version": 2,
  "builds": [
    {
      "src": "apps/frontend/package.json",  
      "use": "@vercel/static-build",
      "config": {
        "distDir": "apps/frontend/.next"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/frontend/$1"
    }
  ]
}
